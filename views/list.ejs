<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>게시물 목록</title>
</head>
<body>
    <h1>게시물 목록</h1>
    <ul>
        <% posts.forEach(function(post) { %>
            <li><strong><%= post.title %></strong> - <%= post.content %></li>
        <% }); %>
    </ul>
</body>
</html>